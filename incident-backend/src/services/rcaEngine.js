/**
 * Claude RCA Engine — Phase 3 implementation
 * Stub so routes can require() this without crashing.
 */

async function generateRCA(incident, logs) {
  throw new Error('RCA Engine not yet implemented — coming in Phase 3');
}

module.exports = { generateRCA };
